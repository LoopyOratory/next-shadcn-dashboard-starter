// Font configuration - fonts are loaded via CSS imports in globals.css and theme CSS
// This replaces next/font/google with CSS-based font loading
// Fonts are available through theme variables defined in @theme inline { --font-sans: ... }

export const fontVariables = '';
